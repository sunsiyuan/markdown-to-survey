node_modules/
dist/
.next/
.env
.env.local
.env*.local
.turbo/
*.tsbuildinfo
.DS_Store

# Private test fixtures — never commit real survey content
fixtures/
.mcpregistry_github_token
.mcpregistry_registry_token

# Local-only planning, strategy, and Claude context — never commit
.local/
.claude/
